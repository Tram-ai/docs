---
title: "交互模式——实时终端会话"
description: "在终端中启动与 Tram 的实时交互会话。提问、探索代码并获取响应——一切都无需离开命令行。"
---

交互模式是使用 Tram 的默认方式。当你在终端中运行 `tram` 时，它会打开一个类似 REPL 的会话，你可以在其中输入提示并实时接收 AI 智能助手的响应。没有浏览器，没有仪表板——只有你的终端和你的代码。

## 启动会话

在任意目录中运行 `tram` 即可开始：

```bash
tram
```

首次运行时，Tram 会在进入交互会话之前引导你完成身份验证设置。配置完成后，后续运行会直接进入提示界面。

## 交互会话示例

启动后，Tram 显示一个提示符，你可以在此输入问题或指令并按回车。以下是一个简短的示例会话：

```
> Explain the overall structure of this project

This project is organized into three main areas: a CLI entry point that handles
argument parsing and session setup, a core agent module that manages conversation
state and tool use, and a set of utilities for reading and summarizing files.
The main entry point is in src/index.ts.

> What is the purpose of the core agent module?

The core agent module coordinates communication between your prompts and the
configured AI model provider. It maintains conversation history across turns
and handles retries and error recovery when the provider returns an error.

> exit
```

## 退出会话

在提示符处输入 `exit` 或 `quit`，或按 `Ctrl+C`，即可结束会话并返回到 shell。

## 高效使用技巧

- **提示要具体。** 不要说"解释这个项目"，而是尝试"解释 src/routes/users.ts 中从 API 路由到数据库的数据流"。具体的提示会产生更有用的答案。
- **直接引用名称。** 当你知道文件名、函数名或类名时，在提示中提及它们——Tram 会利用这些上下文给出更精确的答案。
- **提出后续问题。** 交互模式保持对话持续进行，因此你可以在不重新开始的情况下细化或深入探索。

<Tip>
  从项目的根目录运行 `tram`。Tram 相对于当前工作目录读取文件，因此从根目录启动可以让它获得代码库的最广泛视图。
</Tip>
